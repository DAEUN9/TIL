management:
  info:
    java:
      enabled: true
    os:
      enabled: true
    env:
      enabled: true
#  server:
#    port: 9292
  endpoint:
    health:
      show-components: always
#      show-details: always
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"

info:
  app:
    name: hello-actuator
    company: yh

logging:
  level:
    hello.controller: debug


server:
  tomcat:
    mbeanregistry:
      enabled: true